"use client";

import { HEADER_ACTIONS_SLOT_ID } from "@/components/workspaces/workspace-tab-header-actions";

export function WorkspaceTabHeaderActionsSlot() {
  return <div id={HEADER_ACTIONS_SLOT_ID} className="flex items-center" />;
}
