/**
 * This file adapts the route layer to your service layer.
 * Replace these imports with the actual paths from your project.
 */
import { services } from '@/lib/prisma/service-factory';

export { services };
