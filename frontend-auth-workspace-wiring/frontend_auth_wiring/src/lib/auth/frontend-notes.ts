/**
 * Frontend integration notes:
 * 1. Wrap your root layout in AppSessionProvider
 * 2. Use protected server layouts to load request context and workspace summary
 * 3. Use WorkspaceSwitcher in the app header
 * 4. Use useSignedInUser() in client components that need optimistic UI
 */
export {};
